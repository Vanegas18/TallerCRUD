@model TallerCRUD.Models.Categoria

@{
    ViewData["Title"] = "Detalles";
}

<h1>Detalles</h1>

<div class="d-flex justify-content-center">
    <div class="col-md-6">
        <hr />
        <dl class="row">
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Nombre)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Nombre)
            </dd>
        </dl>
        <div class="form-group mt-4">
            <a asp-action="Edit" asp-route-id="@Model?.CodigoCategoria" class="btn btn-primary">Editar</a>
            <a asp-action="Index" class="btn btn-secondary ml-2">Volver a la lista</a>
        </div>
    </div>
</div>

<style>
    .btn-primary {
        background-color: #ff5722;
        border-color: #ff5722;
    }
    .btn-secondary {
        background-color: #6c757d;
        border-color: #6c757d;
    }
</style>